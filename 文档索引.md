# ServerPilot 项目文档索引

本项目以后以中文文件名作为主要可读文档，同时保留英文文件名作为兼容入口，避免影响 Codex / agents / CI / 外部引用。

## 中文主文档

- `当前轮次状态.md`：当前最新开发状态，短摘要。
- `开发进展.md`：最近每轮实际开发记录。
- `项目交接.md`：长期完整归档。
- `架构说明.md`：架构冻结硬约束。
- `安全边界.md`：安全规则和敏感数据边界。
- `路线图.md`：后续功能路线。
- `Codex开发规则.md`：Codex / agent 开发规则。

## 兼容英文入口

- `AI_BRIEF.md`：兼容 `当前轮次状态.md`。
- `DEV_PROGRESS.md`：兼容 `开发进展.md`。
- `HANDOFF.md`：兼容 `项目交接.md`。
- `ARCHITECTURE.md`：兼容 `架构说明.md`。
- `SECURITY.md`：兼容 `安全边界.md`。
- `ROADMAP.md`：兼容 `路线图.md`。
- `AGENTS.md`：兼容 `Codex开发规则.md`，必须继续保留供 Codex / agents 读取。

## 文档优先级

1. `架构说明.md` / `ARCHITECTURE.md`
2. `安全边界.md` / `SECURITY.md`
3. `Codex开发规则.md` / `AGENTS.md`
4. `当前轮次状态.md` / `AI_BRIEF.md`
5. `开发进展.md` / `DEV_PROGRESS.md`
6. `项目交接.md` / `HANDOFF.md`
7. `路线图.md` / `ROADMAP.md`

架构和安全规则优先级高于功能路线图。`AI_BRIEF.md` / `当前轮次状态.md` 只描述当前进度，不允许覆盖架构冻结规则。

## 必须保留的硬约束

- SSH / Terminal / Monitor / SFTP / SCP / Tunnel / Docker / Local Terminal runtime 相互隔离。
- `DisconnectServer(serverID)` 是服务器级关闭入口。
- 不能重构 workspace state model。
- 不能让 secret 进入 SQLite / logs / frontend state / Wails events。
- Key Vault 只存 metadata。
- `SecretStore` 是敏感信息边界。
- 所有 Wails API 必须使用 typed struct。
- 禁止 `map[string]interface{}` 作为 API contract。
- 新功能必须作为 attachment layer 挂接到现有架构。
- 不允许为了功能重建 SSH runtime。
- 不允许删除测试或放宽测试。
- 文档更新必须写清楚用户可见变化。

## 维护规则

- 中文文档是主要阅读入口，也是文档同步的单一事实源。
- 英文兼容文件不能直接删除，必须通过 `scripts/finalize-after-build.ps1` 从中文主文档同步。
- `AGENTS.md` 和 `AI_BRIEF.md` 必须继续保留。
- `AGENTS.md` 不允许被脚本简单覆盖；它必须继续指向 `Codex开发规则.md`。
- 如果中文主文档和英文兼容入口短期不同步，以中文主文档为准，并运行同步脚本修复。
- `AI_BRIEF.md` 必须保持 120 行以内。

## 文档同步级别

Current：普通 bugfix / UI 调整 / 小功能。

- 更新：`当前轮次状态.md`、`AI_BRIEF.md`、`开发进展.md`、`DEV_PROGRESS.md`。
- 不更新：`项目交接.md`、`HANDOFF.md`、`路线图.md`、`ROADMAP.md`、`架构说明.md`、`ARCHITECTURE.md`、`安全边界.md`、`SECURITY.md`，除非内容确实变化。

Milestone：完整功能阶段完成。

- 更新 Current 文档。
- 额外更新：`项目交接.md`、`HANDOFF.md`、`路线图.md`、`ROADMAP.md`。

Rules：架构、安全或 agent 开发规则变化。

- 只在规则确实变化时更新：`架构说明.md`、`ARCHITECTURE.md`、`安全边界.md`、`SECURITY.md`、`Codex开发规则.md`、`AGENTS.md`。

## 快速同步命令

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/finalize-after-build.ps1 -Mode Current -ExePath build/bin/ServerPilot.exe
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/finalize-after-build.ps1 -Mode Current -Check -ExePath build/bin/ServerPilot.exe
```

文档同步后，如果只有 Markdown 文件发生变化，不需要再次运行完整 Go / npm / Wails build 验证。
