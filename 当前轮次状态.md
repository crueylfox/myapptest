# 当前轮次状态

英文兼容入口：`AI_BRIEF.md`。本文档是当前轮次短摘要的中文主文档，`AI_BRIEF.md` 由 `scripts/finalize-after-build.ps1` 从本文档同步生成。

## 当前状态

- 阶段：Key Vault forced delete and remark field polish。
- “添加密钥”备注 textarea 已复用统一 `.app-textarea` 样式，背景、边框、圆角、字体、placeholder、focus ring 与普通输入框一致；默认 3 行高度，允许垂直 resize，不允许横向 resize。
- 备注保存逻辑未改变；中文、换行和空格仍按原表单字段保存，不进入日志。
- 使用中的 Key Vault 密钥不再被硬性阻止删除；用户点击删除后会先显示受影响服务器数量和名称，再由用户确认“删除密钥并解除绑定”。
- 删除使用中密钥时，后端在同一 SQLite 事务中重新查询真实引用、清空全部受影响服务器的 `key_vault_id`、删除 key record / protected blob；前端展示的 usage 只用于确认，不作为安全边界。
- 受影响服务器记录保留，名称、host、端口、分组和其他配置保留；认证仍保持 private-key / key-vault 模式，不自动切换成密码认证。
- 删除后已建立的 SSH / SFTP / Monitor / Tunnel / Docker / Process / Service Manager 会话不会被强制断开，也不会调用 `DisconnectServer(serverID)`。
- 后续新连接或重连遇到缺失 Key Vault 密钥时返回 typed credential error：`该服务器使用的密钥已被删除，请重新选择密钥。`；连接错误分类保留该中文提示并停止自动重连死循环。
- 删除成功后会尝试清理对应 SecretStore 私钥口令；清理失败只返回非阻塞 warning，不回滚已经完成的数据库删除和解绑。
- legacy path key 删除只删除 ServerPilot 的 Key Vault metadata 和保存的口令引用，不删除、不修改、不擦除用户本地原始私钥文件。
- Backup / Export 边界未改变：protected private-key blob、明文私钥、Key Vault 口令、SecretStore 内容仍不进入标准或完整备份。
- 本轮未修改 DPAPI / Windows 当前用户级保护方式、明文私钥安全边界、SSH Terminal PTY 生命周期、`terminalSessionID`、`WriteTerminal`、HostKeyPolicy、Monitor、SFTP/SCP、Tunnel、Docker、Process、Service Manager、Network Details、Network Diagnostics、Local Terminal、`DisconnectServer(serverID)`、Command History、Alert evaluator 或 `window_state`。
- 未恢复 `build/上传服务器说明.rtf`，未创建 `outputs/`、`work/`、`temp/`。

## 验证

- `gofmt -w app.go app_test.go internal/domain/models.go internal/persistence/store.go internal/persistence/store_test.go internal/credential/resolver.go internal/credential/resolver_test.go internal/connectionerror/classifier.go internal/connectionerror/classifier_test.go`：通过。
- `go vet ./...`：通过。
- `go test ./...`：通过。
- `cmd.exe /c npm run type-check`（frontend）：通过。
- `cmd.exe /c npm run test -- --run`（frontend）：通过，56 个测试文件、603 个测试；既有 jsdom canvas `getContext` 提示仍为非致命。
- `cmd.exe /c npm run build`（frontend）：通过，保留既有 Vite chunk-size warning。
- `wails build`：Windows amd64 通过。
- `git diff --check`：通过。

## 当前 EXE

- 路径：`build/bin/ServerPilot.exe`
- SHA-256：`9F1BD0F84C319097E60D7FC7A6A81A1EB8A2B532B6E1E1B9D6286399884C990D`

## 风险 / 未完成

- 删除正在使用的密钥会让受影响服务器保持“需要重新选择密钥”的配置状态；这符合用户要求，但用户后续必须编辑服务器并选择有效密钥才能重新连接。
- SecretStore 口令清理失败时数据库状态仍保持删除成功，只通过 warning 提醒用户系统凭据清理失败。
- 跨电脑携带私钥材料的独立加密 Key Vault backup/export 暂未实现。
